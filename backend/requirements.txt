fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
PyYAML>=6.0
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0

# Optional: only needed when LLM_PROVIDER=anthropic. The app runs fully
# without it (LLM_PROVIDER defaults to "mock").
anthropic>=0.34
